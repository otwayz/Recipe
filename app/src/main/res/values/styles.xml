<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:homeAsUpIndicator">@drawable/icon_back</item>
        <item name="homeAsUpIndicator">@drawable/icon_back</item>
        <item name="actionBarSize">50dp</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ActionBar.White" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/color_333333</item>
        <item name="android:textColorSecondary">@color/color_333333</item>
        <item name="actionMenuTextColor">@color/color_666666</item>
        <item name="android:actionMenuTextAppearance">@dimen/action_bar_menu_text_size</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">16sp</item>
        <item name="homeAsUpIndicator">@drawable/icon_back_dark</item>
        <item name="android:homeAsUpIndicator">@drawable/icon_back_dark</item>
    </style>
</resources>